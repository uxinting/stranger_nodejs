var fs = require( 'fs' );

exports.write = function( path, msg ) {
    var date = new Date();
    var _msg = date.toString() + ':  ' + msg + '\n';

    fs.appendFile( path, _msg, 'utf-8', function( err ) {
	if ( err ) {
	    console.error( err );
	}
    });
}
